import { createStore } from 'vuex'
import post from './modules/post'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    post
  }
})
